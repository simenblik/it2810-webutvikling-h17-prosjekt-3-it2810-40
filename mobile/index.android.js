import 'expo';
import {AppRegistry} from 'react-native';
import App from './app/index';

AppRegistry.registerComponent('mobileSimsalaPIM', () => App);
AppRegistry.registerComponent('main', () => App);
